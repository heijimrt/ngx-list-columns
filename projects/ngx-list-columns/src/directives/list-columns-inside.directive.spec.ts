import { ListColumnsInsideDirective } from './list-columns-inside.directive';

describe('ListColumnsInsideDirective', () => {
  it('should create an instance', () => {
    const directive = new ListColumnsInsideDirective();
    expect(directive).toBeTruthy();
  });
});
